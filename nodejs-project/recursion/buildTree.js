const buildTree = (list, parent) => // parent is the root of the tree/subtree
                    // TODO

                    
// { autofold
module.exports = {
  buildTree: buildTree
};
// }
